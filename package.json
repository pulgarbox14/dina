{
  "name": "dina-perles",
  "private": true,
  "description": "Outil de compilation du CSS (Tailwind) et copie de Lenis. Inutile sur l'hébergement : les fichiers compilés sont dans public/assets.",
  "scripts": {
    "build": "npm run build:css && npm run build:js",
    "build:css": "tailwindcss -c resources/tailwind.config.js -i resources/css/app.css -o public/assets/css/app.css --minify",
    "build:js": "cp node_modules/lenis/dist/lenis.min.js public/assets/vendor/lenis.min.js",
    "watch:css": "tailwindcss -c resources/tailwind.config.js -i resources/css/app.css -o public/assets/css/app.css --watch"
  },
  "devDependencies": {
    "lenis": "1.3.26",
    "tailwindcss": "3.4.17"
  }
}
